<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
  <h1 class="title"><%= user ? `${user.profile.name}` : "Namaste Nepal" %></h1>
</main>
<%- include('./partials/footer') %>